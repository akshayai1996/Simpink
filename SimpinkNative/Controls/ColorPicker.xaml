<UserControl x:Class="SimpinkNative.Controls.ColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SimpinkNative.Controls"
             x:Name="Root">
    <UserControl.Resources>
        <local:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        <local:ColorToHexConverter x:Key="ColorToHexConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border x:Name="ColorPreview" Width="32" Height="32" CornerRadius="8" BorderBrush="#334155" BorderThickness="1"
                Background="{Binding SelectedColor, ElementName=Root, Converter={StaticResource ColorToBrushConverter}}">
            <Border.Effect>
                <DropShadowEffect Color="Black" ShadowDepth="1" BlurRadius="6" Opacity="0.4"/>
            </Border.Effect>
        </Border>
        <TextBox x:Name="HexTextBox" Grid.Column="1" Margin="8,0,0,0" Height="32" Width="100"
                 Background="#0F172A" BorderBrush="#334155" BorderThickness="1"
                 Foreground="#F8FAFC" FontSize="13" FontWeight="SemiBold" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                 Padding="4,0" Text="{Binding SelectedColor, ElementName=Root, Converter={StaticResource ColorToHexConverter}, Mode=TwoWay}"
                 LostFocus="HexTextBox_LostFocus">
            <TextBox.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </TextBox.Resources>
        </TextBox>
    </Grid>
</UserControl>
