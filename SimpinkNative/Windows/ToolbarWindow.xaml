<Window x:Class="SimpinkNative.Windows.ToolbarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SimpinkToolbar"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        Deactivated="Window_Deactivated">
    <Window.Resources>
        <!-- Glass & Glow Effects matching SimpinkHTML -->
        <DropShadowEffect x:Key="GlassEffect" Color="Black" ShadowDepth="15" BlurRadius="40" Opacity="0.15" Direction="270"/>
        <DropShadowEffect x:Key="ActiveGlowEffect" Color="#4F46E5" ShadowDepth="4" BlurRadius="12" Opacity="0.25"/>
        <DropShadowEffect x:Key="OrbGlowEffect" Color="#EC4899" ShadowDepth="0" BlurRadius="20" Opacity="0.45"/>

        <!-- Custom Slider Style matching HTML range slider -->
        <Style x:Key="SimpinkSlider" TargetType="Slider">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False"/>
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#4F46E5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Background="Transparent" BorderThickness="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Background="Transparent" BorderThickness="0"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="12" Height="12" Fill="#4F46E5" Cursor="Hand"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                            <Border Height="3" Background="#E2E8F0" CornerRadius="1.5" IsHitTestVisible="False" VerticalAlignment="Center" Margin="6,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Base Toolbar Button Style (Light Theme) -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" 
                                CornerRadius="12" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="0" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0A000000"/>
                                <Setter Property="Foreground" Value="#1E293B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#14000000"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active Toolbar Button Style (Indigo Glow) -->
        <Style x:Key="ToolbarButtonActive" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Effect" Value="{StaticResource ActiveGlowEffect}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4338CA"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style (Red Exit Button) -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="#EF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FEE2E2"/>
                    <Setter Property="Foreground" Value="#EF4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Minimized Orb -->
        <Border x:Name="OrbBorder" Width="60" Height="60" CornerRadius="30" Visibility="Collapsed"
                Background="#FCFFFFFF" BorderBrush="#EC4899" BorderThickness="1.5"
                Effect="{StaticResource OrbGlowEffect}"
                Cursor="Hand" MouseLeftButtonDown="Orb_MouseLeftButtonDown"
                ToolTip="Click to expand toolbar">
            <Grid>
                <Image Source="pack://application:,,,/icon.png" Stretch="UniformToFill">
                    <Image.Clip>
                        <EllipseGeometry Center="30,30" RadiusX="26" RadiusY="26"/>
                    </Image.Clip>
                </Image>
            </Grid>
        </Border>

        <!-- Full Light Glass Toolbar Container -->
        <Border x:Name="MainBar" CornerRadius="28" Visibility="Visible"
                Background="#FAFFFFFF" BorderBrush="#0D000000" BorderThickness="1"
                Padding="14,6" Effect="{StaticResource GlassEffect}">
            <StackPanel x:Name="ToolBarPanel" Orientation="Horizontal" VerticalAlignment="Center">
            </StackPanel>
            <Border.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Minimize Toolbar" Click="ContextMinimize_Click"/>
                    <Separator/>
                    <MenuItem Header="Exit Simpink" Click="ContextExit_Click"/>
                </ContextMenu>
            </Border.ContextMenu>
        </Border>
    </Grid>
</Window>