<Window x:Class="SimpinkNative.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SimpinkNative.Controls"
        Title="Simpink Settings"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <local:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        <local:ColorToHexConverter x:Key="ColorToHexConverter"/>

        <Style x:Key="GlassEffect" TargetType="Border">
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" ShadowDepth="8" BlurRadius="40" Opacity="0.5" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Danger Button -->
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#2A1215"/>
            <Setter Property="Foreground" Value="#F87171"/>
            <Setter Property="BorderBrush" Value="#7F1D1D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#991B1B"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#7F1D1D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Primary Save Button -->
        <Style x:Key="SaveButton" TargetType="Button">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#6366F1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#4F46E5" BlurRadius="12" ShadowDepth="0" Opacity="0.4"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#6366F1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3730A3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark Pill ToggleButton -->
        <Style x:Key="PillToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4F46E5"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#818CF8"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="6" Margin="2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4F46E5"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3730A3"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          ClickMode="Press" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateBorder" Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="arrow" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Data="M 0 0 L 4 4 L 8 0 Z" Fill="#94A3B8"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="templateBorder" Property="BorderBrush" Value="#6366F1"/>
                                                <Setter TargetName="arrow" Property="Fill" Value="#F8FAFC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" PopupAnimation="Slide">
                                <Border x:Name="dropDownBorder" Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Margin="0,4,0,0" Padding="4">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="4" Opacity="0.5"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark Glass Slider Style -->
        <Style TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="6" Background="#4F46E5" CornerRadius="3,0,0,3"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="6" Background="#1E293B" CornerRadius="0,3,3,0"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="16" Height="16" Fill="#818CF8" Stroke="#4F46E5" StrokeThickness="2">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="#6366F1" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="#F20F172A" BorderBrush="#334155" BorderThickness="1"
            Effect="{StaticResource GlassEffect}" Padding="28" Width="520">
        <StackPanel>
            <!-- Title Header -->
            <DockPanel Margin="0,0,0,24">
                <TextBlock Text="Simpink Settings" FontSize="22" FontWeight="Bold" Foreground="#F8FAFC" VerticalAlignment="Center"/>
                <Border DockPanel.Dock="Right" HorizontalAlignment="Right" Background="#1E293B" CornerRadius="12" Padding="8,4">
                    <TextBlock Text="PRO DECK" FontSize="11" FontWeight="Bold" Foreground="#818CF8"/>
                </Border>
            </DockPanel>

            <!-- Screenshot Storage Section -->
            <Border Background="#151D2A" CornerRadius="14" BorderBrush="#253248" BorderThickness="1" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Screenshot Folder" FontSize="14" FontWeight="SemiBold" Foreground="#F8FAFC"/>
                        <TextBlock Text="Location where snapshots are saved" FontSize="11" Foreground="#64748B"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Margin="16,0,0,0" Padding="10,6" Cursor="Hand" MouseLeftButtonDown="PickFolder_Click">
                        <TextBlock x:Name="SavePathDisplay" Text="Select Folder..." FontSize="13" Foreground="#818CF8" 
                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Typography Settings Section -->
            <TextBlock Text="Pro Typography Settings" FontSize="14" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,0,8"/>
            <Border Background="#151D2A" CornerRadius="14" BorderBrush="#253248" BorderThickness="1" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,8,0">
                        <TextBlock Text="Font Family" FontSize="11" FontWeight="Medium" Foreground="#94A3B8" Margin="0,0,0,6"/>
                        <ComboBox x:Name="FontFamilyCombo" Height="38">
                            <ComboBoxItem Content="Segoe UI" Tag="Segoe UI"/>
                            <ComboBoxItem Content="Consolas" Tag="Consolas"/>
                            <ComboBoxItem Content="Arial" Tag="Arial"/>
                            <ComboBoxItem Content="Georgia" Tag="Georgia"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="Text Color" FontSize="11" FontWeight="Medium" Foreground="#94A3B8" Margin="0,0,0,6"/>
                        <local:ColorPicker x:Name="TextColorPicker" Height="38"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,16,0,0">
                        <DockPanel Margin="0,0,0,6">
                            <TextBlock Text="Font Size" FontSize="11" FontWeight="Medium" Foreground="#94A3B8"/>
                            <TextBlock Text="{Binding Value, ElementName=FontSizeSlider, StringFormat={}{0:0} pt}" 
                                       DockPanel.Dock="Right" HorizontalAlignment="Right" FontSize="11" FontWeight="Bold" Foreground="#818CF8"/>
                        </DockPanel>
                        <Slider x:Name="FontSizeSlider" Minimum="10" Maximum="100" Value="30" TickFrequency="1" IsSnapToTickEnabled="True" Height="24"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,16,0,0" Orientation="Horizontal">
                        <TextBlock Text="Style Toggles" FontSize="11" FontWeight="Medium" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <ToggleButton x:Name="BoldToggle" Content="Bold (B)" Style="{StaticResource PillToggleButton}" FontWeight="Bold" Width="90" Height="34"
                                      ToolTip="Bold" Click="BoldToggle_Click"/>
                        <ToggleButton x:Name="ItalicToggle" Content="Italic (I)" Style="{StaticResource PillToggleButton}" FontStyle="Italic" Width="90" Height="34"
                                      ToolTip="Italic" Click="ItalicToggle_Click" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Video Recording Quality Section -->
            <TextBlock Text="Video Recording Quality" FontSize="14" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,0,8"/>
            <Border Background="#151D2A" CornerRadius="14" BorderBrush="#253248" BorderThickness="1" Padding="16" Margin="0,0,0,16">
                <ComboBox x:Name="VideoQualityCombo" Height="40">
                    <ComboBoxItem Content="High Definition (10 Mbps)" Tag="hd"/>
                    <ComboBoxItem Content="Pro-Grade (25 Mbps - Crystal Clear)" Tag="pro"/>
                    <ComboBoxItem Content="Standard (5 Mbps)" Tag="std"/>
                </ComboBox>
            </Border>

            <!-- Individual Pen Config Section -->
            <TextBlock Text="Individual Pen Config" FontSize="14" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,0,8"/>
            <Border Background="#151D2A" CornerRadius="14" BorderBrush="#253248" BorderThickness="1" Padding="12" Margin="0,0,0,20">
                <ItemsControl x:Name="PenSettingsList"/>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reset Defaults" Style="{StaticResource DangerButton}" Click="ResetDefaults_Click"/>
                <Button Content="Save &amp; Close" Style="{StaticResource SaveButton}" Margin="12,0,0,0" Click="SaveClose_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>